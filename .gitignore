# kaname 要 — runtime state (NOT committed). The persisted kotoba commit-DAG log is local
# runtime output of the heartbeat; the committed durable artifacts are the seed, the
# web-ingest output (data/ingested-web.kotoba.edn), and data/ingested-pages/.
data/persisted/

# Clojure CLI classpath cache — machine-specific absolute paths, never shared
.cpcache/

# kbb/nbb classpath cache — machine-local build output
.nbb/
