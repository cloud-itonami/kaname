{:paths ["src" "test"]
 :tasks
 {test {:doc "Run kaname cljc actor boundary tests."
        :task (do
                (require 'clojure.test
                         'kaname.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'kaname.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
