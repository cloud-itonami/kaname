;; kaname — canonical actor manifest
{:actor/id "kaname"
 :actor/glyph "要"
 :actor/display-name "要 — Cross-Domain System-of-Systems Leverage Synthesizer"
 :actor/tier :tier-b
 :actor/integrates ["amime" "chie" "hokorobi" "inochi" "junkan" "ossekai" "shiori" "tsumugi"]
 :actor/resources
 {:schema "schema.edn"
  :domain-schema "kotoba/schema.edn"
  :kotoba-app "kotoba.app.edn"
  :seed "data/seed-sos.kotoba.edn"
  :web-mirror "data/ingested-web.kotoba.edn"}
 :actor/mirror-dependencies
 {:resolution :explicit-repository-root-map
  :configuration-env "KANAME_MIRROR_ROOTS_EDN"
  :repositories
  {:amime {:repository "https://github.com/etzhayyim/com-etzhayyim-amime"
           :revision "5fc4c7ef627b3b84c0245fa11a6e674c0b22068e"
           :resource "out/energy-sos.kotoba.edn"}
   :chie {:repository "https://github.com/etzhayyim/com-etzhayyim-chie"
          :resource "data/seed-ai-ecosystem.kotoba.edn"}
   :hokorobi {:repository "https://github.com/etzhayyim/com-etzhayyim-hokorobi"
              :resource "data/seed-finrisk-graph.kotoba.edn"}
   :inochi {:repository "https://github.com/etzhayyim/com-etzhayyim-inochi"
            :resource "data/seed-biosphere-graph.kotoba.edn"}
   :shiori {:repository "https://github.com/etzhayyim/com-etzhayyim-shiori"
            :resource "data/seed-wellbecoming-graph.kotoba.edn"}
   :tsumugi {:repository "https://github.com/etzhayyim/com-etzhayyim-tsumugi"
             :resource "out/woven-graph.kotoba.edn"}}}
 :actor/status :r0-r1-deployed
 :actor/purpose "Non-profit religious-corp META synthesis actor — the system-of-systems (SoS) layer above the power-mirror lineage (tsumugi / keizu / kabuto / chie / shiori / abaki / shionome / busshi / hokorobi / kosatsu / inochi). It JOINS their per-domain 取-concentration into ONE multilayer (multiplex) graph — layers = domains (politics / religion / organization / ideology / economy / ecology / security / wellbecoming / ai / information) — reconciles entities across domains, and computes ON READ (edge-primary; no stored score) the single structural position (the 要 / 律速段階) whose release would most improve resilience across the MAXIMUM number of domains at once: L = C·(V/D)·(1+B)·(1−open), the 要 = argmax L. Concentration ALONE is not the bottleneck; domain VERSATILITY is the SoS discriminator. It then ROUTES the 要 to OPENING (route-around / add-redundancy / decentralize) and PROPOSES the intervention (おせっかい) to ossekai (ADR-2605264000), which carries it consent-bound + on-chain-logged + structural-first. kaname NEVER actuates (no-server-key). Distinct from junkan 循環 (ADR-2605290927, the system-dynamics/CLD view, analysis-only) and ossekai (the actuator). G1 leverage-MAP-not-target-list (structural positions only; natural persons person-excluded; no coordinates). G2 OPENING-only routing (capture/seize/control unrepresentable). G3 おせっかい transparent + consent-bound. G4 non-adjudicating + edge-primary. G5 no thought-policing (ideology/religion = structural interfaces with on-the-record basis; belief-content scoring unrepresentable). G6 synthetic seed; live mirror join G7/Council-gated. clj-native (.cljc, babashka)."
 :actor/verification {:runner "bb test"
                      :tests 63
                      :assertions 248
                      :failures 0
                      :errors 0}
 :actor/adr ["2606172100" "2605192100"]
 :actor/lexicons ["com.etzhayyim.kaname.leveragePoint" "com.etzhayyim.kaname.osekkaiProposal"]}
